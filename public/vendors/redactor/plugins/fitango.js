(function($R)
{
    $R.add('plugin', 'fitangoimage', {

        // construct
        init: function(app)
        {
            // define redactor app
            this.app = app;

            // your code
        }
    });
})(Redactor);